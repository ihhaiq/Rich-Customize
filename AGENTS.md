# AGENTS.md — بوت محرر الرسائل الغنية

تعليمات العمل على مستودع `ihhaiq/Rich-Customize`، وتسري على كامل المشروع ما لم توجد تعليمات أقرب إلى الملف.

آخر مراجعة: **2026-09-10**. يستند سجل التعديلات أدناه إلى `main` عند commit [932256a](https://github.com/ihhaiq/Rich-Customize/commit/932256a13ccc1dfa63ba8e97448611ee9b53a914).
هذا سجل للتغييرات الموجودة في المستودع، ولا يثبت وحده أن إصدار Railway المنشور يطابقه.

## المشروع والتشغيل

- بوت Telegram لبناء الرسائل الغنية من بلوكات، مع محرر Mini App وصفحات محفوظة ونشر إلى القنوات والمجموعات.
- بيئة التشغيل المعتمدة في Docker وCI هي Python 3.12. استخدم الاعتماديات المثبتة في `requirements.txt` و`requirements-dev.txt`؛ Aiogram مربوط بـcommit محدد لدعم واجهات المشروع.
- نقطة التشغيل: `python main.py`. راجع `README.md` و`.env.example` للإعدادات، ولا تضع بيانات اعتماد فعلية في التوثيق.
- المسار `/miniapp` للمحرر، و`/healthz` لفحص صحة الخدمة. أوامر الواجهة المهمة: `/editor` و`/dev` و`/draft`.

## خريطة الكود

| المسار | المسؤولية |
| --- | --- |
| `app/editor/` | نموذج البلوكات، السجل الموحد، البناء، الاستيراد، عمليات المستند، المسودات والتراجع والإعادة. |
| `app/routers/` | استقبال أوامر Telegram وcallbacks، موزعة حسب المحرر والبلوكات والأزرار والصفحات والنشر. |
| `app/keyboards/` | بناء لوحات الأزرار حسب الميزة. |
| `app/services/` | التحويل والعرض والوسائط والأزرار وسجلات الصفحات والمحادثات وقالب العرض. |
| `app/webapp/` | خادم Mini App والمصادقة والصفحات والروابط والأزرار ورفع الوسائط. |
| `app/miniapp.py` | واجهة استيراد عامة صغيرة تحافظ على توافق مستوردي وظائف Mini App. |
| `app/miniapp_static/` | ملفات HTML وCSS وJavaScript للمحرر. |
| `app/lang/` و`app/i18n*.py` | حزم اللغات والكتالوجات والمفاتيح الدلالية وطبقة الترجمة. |
| `app/storage/hybrid.py` | PostgreSQL، ملفات JSON الاحتياطية، وإعادة الاتصال والمزامنة. |
| `tests/` و`.github/workflows/tests.yml` | الاختبارات وفحوص CI. |

تفاصيل الحدود المعمارية في `docs/editor_architecture.md`، وقواعد الترجمة في `docs/I18N_GUIDE.md` و`app/lang/README.md`.

## قواعد التعديل

1. ضع السلوك في الوحدة المسؤولة عنه. ملفات تجميع Routers تجمع الوحدات فقط؛ لا تسجّل المعالج مرتين ولا تنقل المعالجات أو تستبدل الدوال وقت التشغيل لتجاوز حدود الوحدات. لا تُعد الملفات القديمة مثل `app/routers/editor_legacy.py` و`app/services/factory.py`.
2. مرّر عمليات تغيير البلوكات عبر `app/editor/workflow.py`، واحفظ عبر `app/editor/draft_store.py` مع لقطات `app/editor/history.py`. حافظ على معرّفات البلوكات وحقول المصدر وTelegram entities. عند تحويل بلوك native إلى generated احذف حقول native القديمة عند حد الاستبدال.
3. استخدم `t("namespace.key")` للنصوص الجديدة، وأضف المفاتيح لكل اللغات المدعومة مع placeholders متطابقة. لا تترجم محتوى المستخدم أو أسماء قنواته. يبقى `tr()` للأجزاء القديمة أثناء الترحيل؛ لا تعتبر تنظيف التوطين دليلًا على انتهاء الترحيل كله.
4. حافظ على `app/miniapp.py` كواجهة صغيرة، واجعل التنفيذ داخل `app/webapp/`. استخدم ملفات الواجهة ذات الأسماء الحالية، وراجع ترتيب تحميلها في `app/miniapp_static/index.html` عند نقلها أو دمجها.
5. لا تغيّر ترتيب الأزرار أو أسماءها أو وظيفة ميزة أخرى ضمن مهمة تنظيف. حافظ على التراجع والإعادة والصفحات والمعاينة وصلاحيات المطوّر والمصادقة أثناء أي تعديل.
6. حدّث هذا الملف عند تنفيذ تغيير مهم: التاريخ، السلوك الناتج، الملفات الرئيسية، ورابط commit أو PR المدمج. لا تسجّل طلبًا غير منفذ بوصفه ميزة مكتملة، ولا تُبقِ وصفًا لتغيير تراجع عنه commit لاحق.

## سلوك يجب الحفاظ عليه

- **الجدول:** تحديد خلية يُظهر مقبض خيارات على يسارها للخلية والصف والعمود. النقاط الست على يمين البلوك مخصصة لتحريك الجدول؛ لا تغيّرها ضمن إصلاح أدوات الخلايا. لا تجعل الضغط المطوّل الوسيلة الوحيدة لفتح الأدوات. التنفيذ الحالي في `live_preview.js` والتنسيق في `editor_features.css` داخل `app/miniapp_static/`.
- **ملفات أدوات الجدول:** حُذفت ملفات `table_cell_tools.js` و`table_cell_tools.css`؛ نُقل السلوك إلى الملفات الحالية وأزيل تحميل المسارات المحذوفة من `interactions.js`. لا تُعد روابط التحميل الميتة.
- **قالب كل البلوكات:** زر `r:showcase` وأمر `/draft` يبنيان رسالة غنية واحدة عبر `send_all_blocks_showcase` في `app/services/showcase.py`. يولّد البوت المحتوى التجريبي ويستعين بوسائط مكتبة القناة؛ لا تستبدل ذلك بإرسال منشورات القناة كما هي. حافظ على اسم الزر ووظيفته ورسالة فشل القالب الخاصة.
- **رسائل الترحيب والأخطاء:** الرسائل غير المعروفة في الخاص عندما تكون حالة FSM فارغة تعرض الترحيب. لا تعمم هذا السلوك على أخطاء المعاينة أو القالب أو حالات المحرر النشطة.
- **الثيم والزجاج السائل:** الوضع العادي يستخدم نفس عدسات Liquid Glass والتمويه للأزرار والأشرطة والقوائم، مع ألوان ثيم Telegram ونص وظلال مناسبة لها. زر الصاعقة يبدّل إلى الأسود الداكن اختيارياً ويحفظ الاختيار في `localStorage` بالمفتاح `richCustomizeLiquidGlassDark`. التنسيق المشترك ولوحة الأسود في `liquid_glass_dark.css`، والتبديل في `liquid_glass_dark.js` داخل مجلد الواجهة؛ احتُفظ باسم الملف ومفتاح التخزين للتوافق. حافظ على تخفيف التمويه في `mobile-performance` وعلى تحديث إصدار CSS في `index.html` عند تغييره.
- **لوحة المطوّر:** زرا «فحص قاعدة البيانات» و«تحديث قناة المعاينة» داخل بلوك أزرار غني في الرسالة. حافظ على `dev:database:check` و`dev:showcase:refresh` وفحص صلاحية المطوّر؛ لا تحولهما إلى زرين Inline ضمن تغيير آخر.
- **التخزين:** PostgreSQL مخزن أساسي عند توفر الاتصال، مع نسخة JSON وfallback تلقائي. حافظ على مسارات `*_STATE` وعلى مزامنة التغييرات المحلية عند إعادة الاتصال. بقاء JSON بعد إعادة نشر Railway يحتاج Volume؛ لا تصف fallback بأنه دائم دون تخزين دائم.

## آخر التعديلات على main

التواريخ في الجدول بتوقيت UTC. رُوعي أثر الإصلاحات اللاحقة على التغييرات السابقة.

| التاريخ | التعديل الموجود حالياً | المرجع |
| --- | --- | --- |
| 2026-09-10 | تعميم تأثير Liquid Glass على الوضع العادي باستخدام ألوان Telegram، مع مشاركة التنسيق والتمويه بين الوضعين، وضبط وضوح النص والظلال، والحفاظ على زر الصاعقة وتفضيل الأسود المحفوظ. تحديث إصدار ملف CSS في `index.html`. | [932256a](https://github.com/ihhaiq/Rich-Customize/commit/932256a13ccc1dfa63ba8e97448611ee9b53a914) |
| 2026-09-10 | إرجاع أدوات الخلية والصف والعمود: المحاذاة، التظليل، الدمج وفكه، إضافة وحذف الصفوف والأعمدة، وحدود الجدول والصفوف المخططة والوضع المضغوط. دمج الأدوات في ملفات الواجهة الحالية وإزالة تحميل الملفات المحذوفة. | [PR #43](https://github.com/ihhaiq/Rich-Customize/pull/43) · [5aab28b](https://github.com/ihhaiq/Rich-Customize/commit/5aab28b8771d6ffdd9647d7b9e56668d0d1db868) |
| 2026-09-09 | نقل خادم Mini App إلى `app/webapp/` والإبقاء على واجهة `app/miniapp.py`، وترحيل واجهات إضافية إلى مفاتيح ترجمة دلالية، وفصل بيانات الكتالوج المشترك وحذف جسر الترجمة الصيني القديم مع اختبارات حماية. | [a0d5a0b](https://github.com/ihhaiq/Rich-Customize/commit/a0d5a0beb467dba4239bc0f4e082c4b276d270b1) |
| 2026-09-09 | تنظيم أسماء ملفات Mini App حسب الميزة، وفصل قائمة البلوكات عن الثيم الداكن، وتنظيف كود قناة العرض غير المستخدم. أصلحت إعادة أدوات الجدول في 10 سبتمبر أثر حذف ملفاتها في هذه الخطوة. | [4826efb](https://github.com/ihhaiq/Rich-Customize/commit/4826efbe635a9c7f81bd6fb1951d620fdf4e97fb) |
| 2026-09-09 | استعادة بناء قالب كل البلوكات كرسالة غنية مولدة، مع استخدام وسائط القناة وإضافة اختبار لمسار القالب. | [8f0382d](https://github.com/ihhaiq/Rich-Customize/commit/8f0382da56b492e4a1da6a27d1ce08f8d4f516c0) · [2ed3a9c](https://github.com/ihhaiq/Rich-Customize/commit/2ed3a9cba0bb0554f6bf11497436fca010b6e55f) |
| 2026-09-09 | إضافة ثيم Liquid Glass أسود داكن اختياري وزر صاعقة للتبديل مع حفظ الاختيار محلياً. | [d5e993e](https://github.com/ihhaiq/Rich-Customize/commit/d5e993ed743010730b52aca0fd9181e69b36a2c7) · [632a4e9](https://github.com/ihhaiq/Rich-Customize/commit/632a4e9f66437a59ef699a00af691a96f39f77b4) |
| 2026-09-09 | إرجاع واجهة زر قالب كل البلوكات، واستعادة رسالة فشله الخاصة، وعرض الترحيب للرسائل غير المعروفة في الخاص عندما لا توجد حالة نشطة. | [0eb04a8](https://github.com/ihhaiq/Rich-Customize/commit/0eb04a88e97948d087ea8266509f59119c61c4d0) · [d691c9e](https://github.com/ihhaiq/Rich-Customize/commit/d691c9e4e9634ef4d5a175bb8dc82dc8cd35fe4f) · [a05db1a](https://github.com/ihhaiq/Rich-Customize/commit/a05db1add5f9ab4bcaebb43ec376580454250be1) |
| 2026-09-09 | إضافة كاش دائم لقناة المعاينة وزر غني لتحديثها، وإدراج حالتها ضمن النسخ الاحتياطية. بقي الكاش بعد استعادة بناء القالب المولد. | [bce94d7](https://github.com/ihhaiq/Rich-Customize/commit/bce94d7d7cf0d62177bb179ebd7bf535087fd5c9) |
| 2026-09-09 | تحويل «فحص قاعدة البيانات» في `/dev` إلى زر غني داخل الرسالة وإضافة اختبار له. | [45df0a6](https://github.com/ihhaiq/Rich-Customize/commit/45df0a617ca5e55d27641c3cec57e2e1448722d1) · [35e8581](https://github.com/ihhaiq/Rich-Customize/commit/35e85813fd11958a18763dde8983c4200017f471) |
| 2026-09-09 | إضافة PostgreSQL مع fallback إلى JSON، استيراد الحالة المحلية إلى قاعدة فارغة، وحفظ حالة التطبيق وFSM وإعادة الاتصال والمزامنة. | [PR #40](https://github.com/ihhaiq/Rich-Customize/pull/40) · [73cb4bc](https://github.com/ihhaiq/Rich-Customize/commit/73cb4bce1d9e6595b261c8449c1ef2e4b499932d) |
| 2026-09-09 | تنظيف التوطين وتقوية مسارات المحرر، وهو الأساس الذي تبعته تعديلات التخزين والواجهة والتقسيم المذكورة أعلاه. | [PR #39](https://github.com/ihhaiq/Rich-Customize/pull/39) · [c4ecf6a](https://github.com/ihhaiq/Rich-Customize/commit/c4ecf6a46d7dcbdd7239bdcfaf38a808009a3736) |

## التحقق من العمل

لتثبيت أدوات التطوير:

```bash
python -m pip install -r requirements-dev.txt
```

الفحوص المعتمدة في CI:

```bash
python -m ruff check .
python -m mypy app main.py
python -m compileall -q app main.py tests
python -m pytest -q
```

- اختر اختبارات السلوك المتأثر أثناء التطوير. من المراجع المهمة: `tests/test_hybrid_storage.py`، و`tests/test_showcase_channel.py`، و`tests/test_miniapp_static.py`، و`tests/test_miniapp_api.py`، و`tests/test_cleanup_i18n_webapp.py`.
- لتعديل أدوات الجدول، افحص عملياً تحديد الخلية وفتح الأدوات على يسارها، إضافة وحذف الصفوف والأعمدة، حفظ النتيجة، وبقاء سحب البلوك بالنقاط الست.
- لتعديل التوطين، افحص العربية واتجاه RTL ولغة غير عربية، مع اختبارات التغطية الحالية.
- لتعديل التوثيق فقط، راجع المسارات والروابط وتطابق الوصف مع الكود وسجل Git؛ لا تنسب إلى العمل اختبارات تشغيل لم تُنفذ.
